#import "../template.typ": *

#cvSection("Проекты")

#cvProject(
  title: [#link("https://github.com/tensorush/bookkeeper")[\u{f092} #hSpc() #underline("bookkeeper")]],
  description: [#emoji.hamster #emoji.books Серверная часть для бухгалтерской службы.]
)

#cvProject(
  title: [#link("https://github.com/tensorush/zig-msgpuck")[\u{f092} #hSpc() #underline("msgpuck")]],
  description: [#emoji.lizard #emoji.mail Zig-порт формата бинарной сериализации msgpuck, реализованный командой Tarantool.]
)

#cvProject(
  title: [#link("https://github.com/tensorush/zig-uuid")[\u{f092} #hSpc() #underline("uuid")]],
  description: [#emoji.lizard #emoji.id Zig-реализация всех версий универсального уникального идентификатора (UUID).]
)

#cvProject(
  title: [#link("https://github.com/tensorush/vault")[\u{f092} #hSpc() #underline("vault")]],
  description: [#emoji.hamster #emoji.lock Telegram-бот для безопасного хранения паролей.]
)
